

class UserService {

    /**
     * 
     * getMatches
     * 
     * @param {string} userId 
     * @param {string} action 'wants, give'
     * @param {string[]} tags 
     * 
     * When given a `userId` and list of `tags`, this function will
     * find other users that can help with those tags
     * 
     */
    getMatches(userId, action, tags) {
        // pull in data filtered by this
        // return it
    }
}

export default UserService
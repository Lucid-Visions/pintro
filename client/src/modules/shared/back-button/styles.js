import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    image: {
        height: 15,
        width: 20,
        resizeMode: "contain",
        marginLeft: -10
    }
});

export default styles;